services:
  - type: web
    name: riesgo-morosidad-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: API_KEY
        value: Co05@Lo30%Te28*
